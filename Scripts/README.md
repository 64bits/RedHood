# Generating the map

Use the Blender script in the Blender scripts folder to first get the map.csv.
Then run the scripts here to get the maps. The base has to built manually, I
am using Affinity to do it.